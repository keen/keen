import { FC } from 'react';
import { IconType } from '../types';
declare type Props = {
  width?: number;
  height?: number;
  fill?: string;
  type: IconType;
};
export declare const Icon: FC<Props>;
export default Icon;
