import { colors } from '@keen.io/colors';

import { Typography } from '../../types';

export const labelTypography: Typography = {
  fontSize: 14,
  fontFamily: 'Lato Regular, sans-serif',
  fontWeight: 'normal',
  fontStyle: 'normal',
  fontColor: colors.black['100'],
};
