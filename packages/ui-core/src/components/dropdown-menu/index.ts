import { DropdownMenu as Container } from './dropdown-menu.component';
import { Item, Header, Divider } from './components';

const DropdownMenu = {
  Container,
  Header,
  Divider,
  Item,
};

export default DropdownMenu;
