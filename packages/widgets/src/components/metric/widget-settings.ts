import { colors } from '@keen.io/colors';

export const metricWidgetSettings = {
  card: {
    backgroundColor: colors.white['500'],
    borderRadius: '0px',
    border: 'none',
    hasShadow: true,
  },
};
