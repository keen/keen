import { FC } from 'react';
import { TextSettings } from '../types';
declare type Props = {
  title: TextSettings;
  subtitle: TextSettings;
};
export declare const WidgetHeading: FC<Props>;
export default WidgetHeading;
