export const TO_RADIANS = Math.PI / 180;
export const TO_DEGREES = 180 / Math.PI;
export const INTERVAL_UNITS: { [key: string]: string } = {
  minutely: 'minute',
  hourly: 'hour',
  daily: 'day',
  weekly: 'week',
  monthly: 'month',
  yearly: 'year',
};
