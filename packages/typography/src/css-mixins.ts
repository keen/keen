import { css } from 'styled-components';

export const RESET_SPACING = css`
  margin: 0;
  padding: 0;
`;

export const TEXT_ELLIPSIS = css`
  white-space: nowrap;
  text-overflow: ellipsis;
  overflow: hidden;
`;
