import { fetchMapTopology, MapType } from './map.utils';
import { generateChoropleth, GeoProperty } from './chart.utils';

export type { GeoProperty, MapType };

export { fetchMapTopology, generateChoropleth };
