import TableChart, {
  Props as TableChartSettings,
} from './table-chart.component';

import { TableEvents, ColumnSelection, ValueFormatter } from './types';

export { TableChart };
export type {
  TableChartSettings,
  TableEvents,
  ColumnSelection,
  ValueFormatter,
};
