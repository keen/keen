import { Layout } from '@keen.io/ui-core';
export declare const calculateMarkPosition: ({
  layout,
  x,
  y,
  width,
  height,
}: {
  layout: Layout;
  x: number;
  y: number;
  width: number;
  height: number;
}) => {
  x: number;
  y: number;
};
