import React, { FC } from 'react';
declare type Props = {
  visible: boolean;
  children: React.ReactNode;
  x: number;
  y: number;
};
declare const ChartTooltip: FC<Props>;
export default ChartTooltip;
