import React from 'react';
declare type Props = {
  children: React.ReactNode;
  dx: string | number;
  dy: string | number;
};
declare const Text: ({ children, dx, dy }: Props) => JSX.Element;
export default Text;
